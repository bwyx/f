{
  "name": "f",
  "version": "0.0.1",
  "description": "f",
  "exports": "./release/index.js",
  "scripts": {
    "clean": "rm -rf {dist,release}/*",
    "dev": "npm run clean && NODE_ENV=development tsc-watch --onSuccess \"node dist/server.js\" | npx pino-colada",
    "build": "npm run clean && NODE_ENV=production tsc --outDir release",
    "test": "npm run clean && tsc && mocha dist/**/*.spec.js"
  },
  "author": "Bayu Kurnia",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "env-schema": "^3.5.2",
    "fastify": "^3.27.4",
    "fastify-autoload": "^3.11.0",
    "fastify-sensible": "^3.1.2",
    "is-docker": "^3.0.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "@types/node": "^17.0.21",
    "chai": "^4.3.6",
    "mocha": "^9.2.2",
    "pino-colada": "^2.2.2",
    "tsc-watch": "^4.6.2",
    "typescript": "^4.6.2"
  },
  "node": ">=16.0.0"
}
