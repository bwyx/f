APP_NAME=f
APP_KEY=
APP_ENV=development
APP_PORT=5000
APP_DEBUG=false

FRONTEND_DOMAIN=

# see https://github.com/lukeed/ms#usage
TOKEN_ACCESS_EXPIRATION=15m
TOKEN_REFRESH_EXPIRATION=1d

TRUST_PROXY=false

DB_CONN=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_USER=
DB_PASS=
DB_NAME=

DATABASE_URL=${DB_CONN}://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}
